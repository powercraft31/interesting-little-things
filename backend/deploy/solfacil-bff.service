# RETIRED — DO NOT USE
#
# This legacy systemd unit belonged to the old host bare-run path:
#   - backend/scripts/local-server.ts
#   - host BFF on :3000
#   - host PostgreSQL on :5432
#
# As of 2026-03-19, local Solfacil runtime has been closed to:
#   - public entry: http://152.42.235.155 (nginx :80 reverse proxy)
#   - BFF bind:    http://127.0.0.1:3100
#   - DB bind:     postgresql://127.0.0.1:5433/solfacil_vpp
#
# DO NOT install or re-enable a host systemd BFF service from this file.
# The canonical local runtime is Docker-only via /tmp/solfacil/docker-compose.yml.
