# v6.10.1 Storage-Retention Hardening — Plan Review (Claude, round 2)

Reviewer: closing review of tightened plan
Date: 2026-04-19 UTC
Plan under review: `docs/v6.10.1-storage-retention-hardening-plan.md`
Claude Code session: `a2ff98b4-9b34-4a0e-b4c0-681c6f8b7961`

---

## 1. Overall judgment

The tightened plan is now ready for direct implementation.

It is faithful to the accepted REQ and binding v6.10.1 design set, carries every fixed guardrail from the review brief, and resolves the round-1 plan-review tightenings with explicit implementation commitments rather than posture language. It does not reopen design, does not expand frontend scope, and keeps rollback additive/behavioral.

## 2. What is now resolved

- **REQ/design fidelity is explicit and stable.** The plan stays aligned with the accepted storage-governance model: `revenue_daily` remains preserve-in-hot/no TTL/no archive; Docker logging stays `json-file` with `max-size: 10m` and `max-file: 5` across the four Solfacil containers; executor shape remains one hourly shared extension of the existing v6.10 retention job.
- **The `device_command_logs` eligibility predicate is now exact.** The plan commits the terminal rule as `result IN ('success','fail','timeout') OR resolved_at IS NOT NULL` and explicitly protects `pending`, `dispatched`, and `accepted` from age-only sweeping.
- **The executor failure path is no longer optional.** `storage.retention.executor.failed` is now required and tied to `backend/src/shared/runtime/contract.ts`.
- **The M9/shared storage-policy registry is now concretely targeted.** `backend/src/shared/runtime/storage-policy.ts` is pinned as the canonical checked-in registry file, with matching test ownership under shared tests.
- **Batching is now committed.** The plan sets a default ceiling of 5,000 rows per phase per run instead of leaving chunking to implementer guesswork.
- **Archive idempotence is now committed.** The plan names preserved-source `UNIQUE` plus `INSERT ... ON CONFLICT DO NOTHING` as the archive rerun safety mechanism.
- **Hourly cadence is now pinned in a real file target.** `backend/scripts/local-server.ts` must keep `RUNTIME_RETENTION_INTERVAL_MS` defaulted to `60 * 60 * 1000`.
- **WS5/WS6 scope control is now materially tighter.** Edit targets are separated from review surfaces, so compatibility verification is no longer framed as a broad production-file edit sweep.
- **Integration-lane ownership is now concrete.** The plan names `backend/test/shared/runtime-retention.test.ts` or `backend/test/shared/runtime-retention-storage.test.ts` as the proving surface for archive-first, rerun-idempotence, pending-row protection, and backfill/alarm retention integration coverage.
- **Gate language is now falsifiable.** The acceptance matrix ties the critical commitments to named files, commands, and test lanes instead of leaving them as reviewer expectations.

## 3. Remaining blocking issues

None.

There is no remaining blocker that should force another review cycle before implementation. The plan is implementation-ready as written.

## 4. Non-blocking cautions

- **`backend/src/shared/runtime/persistence.ts` remains conditional.** That is acceptable because the plan limits the touch to query-plumbing only if it belongs there; this is an implementation-placement judgment, not a design gap.
- **Capacity-budget appendix detail is still optional rather than mandatory.** The accepted design already carries the budgeting model, so this does not block implementation, but release notes should add concrete rows/day or MB/day estimates if they are easy to produce.
- **Residual ambiguity is confined to implementation detail.** Exact preserved-source key shape per archive table, final helper-index list after schema inspection, exact counter field names, and whether to split storage integration tests into a sibling file are all legitimate coding-time decisions under the accepted design.
- **Some gate evidence depends on tests that do not yet exist.** That is correct for a plan; the important point is that the plan now names the required proving files and case categories explicitly.

## 5. Decisions still requiring Alan

None.

No product, policy, or design decision remains open. The remaining judgments are implementation-local and already bounded by the accepted plan/design guardrails.

## 6. Recommendation on readiness for implementation

Proceed directly to implementation.

No further plan review cycle is required. The tightened plan now has the required rigor, evidence shape, file-target realism, and scope control to be executed directly against v6.10.1 without reopening design.
