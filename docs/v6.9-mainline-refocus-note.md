# v6.9 Mainline Re-Focus Note

Status: return focus to v6.9 mainline now. DB track is parked unless a reopen condition fires.

## Executive judgment

v6.9 is not blocked by more DB archaeology.
The shortest correct path now is:
1. close the remaining release-critical security contract gaps
2. complete deployment boundary work
3. run the acceptance matrix
4. stop

Do not expand scope beyond items that are still directly required by the v6.9 REQ acceptance criteria.

## Current state by workstream

### WS-B — Backend Auth Boundary
Partially implemented, but not fully release-ready yet.

Confirmed present in repo:
- cookie-first auth middleware exists
- session endpoint exists
- login browser/machine split exists
- secret validation exists
- rate-limit store and middleware exist
- security headers middleware exists

Evidence:
- `backend/src/bff/middleware/auth.ts`
- `backend/src/bff/handlers/auth-login.ts`
- `backend/src/bff/handlers/auth-session.ts`
- `backend/src/shared/auth/validate-jwt-secret.ts`
- `backend/src/bff/middleware/rate-limit.ts`
- `backend/src/bff/middleware/security-headers.ts`

But one release-critical contract gap is still visible:
- browser login cookie is missing `secure: true` in `auth-login.ts`
- logout clear path is also missing `secure: true`

Why this matters:
- production cookie name is `__Host-solfacil_session`
- `__Host-` cookies require `Secure`
- without `Secure`, the production browser contract is not actually correct

Evidence:
- `backend/src/bff/handlers/auth-login.ts`
- tests in `backend/test/bff/auth-login.test.ts` also do not assert `secure: true`

### WS-F — Frontend Security
Substantial progress exists, but CSP cleanup is not fully finished across the actual app surface.

Already improved:
- login moved to `js/login.js`
- self-hosted assets exist
- localStorage auth token path has been removed from the main auth flow
- major admin routes were already live-verified earlier

But remaining CSP blockers still exist in live app files:
- `frontend-v2/index.html` still contains inline `onclick` on the hamburger button
- `frontend-v2/js/components.js` still emits inline `onclick="location.reload()"`
- `frontend-v2/js/p2-devices.js` still emits inline `onclick="DevicesPage._toggleDiagPanel(this)"`

Why this matters:
- v6.9 target CSP is `script-src 'self'` without broad inline exceptions
- these inline handlers are still part of the main app surface, not just old mock files

Evidence:
- `frontend-v2/index.html`
- `frontend-v2/js/components.js`
- `frontend-v2/js/p2-devices.js`

### WS-D — Deployment Security Boundary
This appears to be the least-completed workstream.

Current repo evidence shows deployment slice is not yet aligned with the plan:
- `docker-compose.yml` does NOT yet include `solfacil-redis`
- `docker-compose.yml` does NOT yet provide `JWT_SECRET` to `solfacil-bff`
- `docker-compose.yml` does NOT yet provide `RATE_LIMIT_REDIS_URL` to `solfacil-bff`
- `.env.example` does NOT yet document `JWT_SECRET`, `RATE_LIMIT_REDIS_URL`, or `LEGACY_BROWSER_BEARER`
- `backend/scripts/local-server.ts` does NOT yet show `app.set('trust proxy', 1)`

Why this matters:
- abuse-control production topology is not deployment-closed without Redis/env wiring
- trusted-proxy correctness is part of the cookie/security boundary plan
- live acceptance on the public surface cannot be considered complete until WS-D is real

Evidence:
- `docker-compose.yml`
- `.env.example`
- `backend/scripts/local-server.ts`
- target state documented in `docs/v6.9-deployment-security-boundary-plan.md`

### WS-I — Integration & Acceptance
Not done yet.

Reason:
- acceptance requires the combined truth of backend + frontend + deployment
- current repo still shows unresolved items in all three upstream workstreams
- therefore final acceptance matrix would be premature right now

Reference:
- `docs/v6.9-security-plan.md`
- `docs/v6.9-security-design.md`

## Must-do before calling v6.9 complete

These are the minimal remaining items that still belong to v6.9 proper.

### Must-do 1 — Fix production cookie contract
- add `secure: true` for production browser cookie set/clear path
- keep dev-mode exception explicit if needed
- update tests to assert the right prod/dev split

Files:
- `backend/src/bff/handlers/auth-login.ts`
- `backend/test/bff/auth-login.test.ts`

### Must-do 2 — Finish remaining CSP blocker cleanup on actual app surface
- remove inline `onclick` from `index.html`
- remove inline handler HTML emitted by `components.js`
- remove inline handler HTML emitted by `p2-devices.js`
- re-verify no main-surface inline-handler blockers remain

Files:
- `frontend-v2/index.html`
- `frontend-v2/js/components.js`
- `frontend-v2/js/p2-devices.js`

### Must-do 3 — Close deployment boundary
- add Redis service to local deployment model
- wire `JWT_SECRET` and `RATE_LIMIT_REDIS_URL`
- update `.env.example`
- add `app.set('trust proxy', 1)` in `local-server.ts`
- verify startup and fail-fast behavior under the intended production-like path

Files:
- `docker-compose.yml`
- `.env.example`
- `backend/scripts/local-server.ts`

### Must-do 4 — Run bounded acceptance, not open-ended exploration
Minimum acceptance after the above:
- browser login flow
- session endpoint flow
- cookie attributes on live/local surface
- rate-limit trigger and `Retry-After`
- no auth token in localStorage
- no `Authorization: Bearer` on browser-originated requests
- no external CDN/font dependency on actual shipped pages
- CSP/security headers present on target live surface

Reference:
- `docs/v6.9-security-plan.md` §8

## Explicitly defer

These are valuable but should not block v6.9 once the must-do list above is complete:
- CI integration for local DB rebuild-check
- deeper DB cleanup beyond reopen conditions
- broader frontend cleanup unrelated to v6.9 security closure
- historical migration archaeology
- phase-2 security items like MFA / refresh token rotation / dual-secret rotation

## Shortest correct path from here

1. Backend: fix production cookie `Secure` contract
2. Frontend: remove remaining inline handler CSP blockers in real app files
3. Deployment: wire Redis + env vars + trust proxy + docs
4. Run the acceptance matrix on the intended surface
5. Document residual risk only if anything intentionally remains bounded
6. Stop and return to release decision

## Stop rule

Do not widen scope beyond the four must-do items above unless a new issue clearly violates a current v6.9 acceptance criterion.

That is the line between finishing the release correctly and drifting into “deepening because we can.”
